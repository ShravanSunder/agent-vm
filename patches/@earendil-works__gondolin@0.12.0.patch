diff --git a/dist/src/qemu/net.js b/dist/src/qemu/net.js
index 096b146191c6f2d554a53ea2df43c5fe24405f36..533ecb87bb8818ba7387bdc55f74c562b4571730 100644
--- a/dist/src/qemu/net.js
+++ b/dist/src/qemu/net.js
@@ -290,19 +290,16 @@ export class QemuNetworkBackend extends EventEmitter {
                 onTcpResume: (message) => this.handleTcpResume(message),
             },
             allowTcpFlow: (info) => {
+                const session = this.tcpSessions.get(info.key);
+                if (session?.mappedTcp) {
+                    session.protocol = "tcp";
+                    return true;
+                }
                 if (info.protocol === "tcp") {
-                    const session = this.tcpSessions.get(info.key);
-                    const allowed = Boolean(session?.mappedTcp);
-                    if (!allowed) {
-                        if (this.options.debug) {
-                            this.emitDebug(`tcp blocked ${info.srcIP}:${info.srcPort} -> ${info.dstIP}:${info.dstPort} (${info.protocol})`);
-                        }
-                        return false;
-                    }
-                    if (session) {
-                        session.protocol = "tcp";
+                    if (this.options.debug) {
+                        this.emitDebug(`tcp blocked ${info.srcIP}:${info.srcPort} -> ${info.dstIP}:${info.dstPort} (${info.protocol})`);
                     }
-                    return true;
+                    return false;
                 }
                 if (info.protocol === "ssh") {
                     const allowed = isSshFlowAllowed(this, info.key, info.dstIP, info.dstPort);
@@ -312,7 +309,6 @@ export class QemuNetworkBackend extends EventEmitter {
                         }
                         return false;
                     }
-                    const session = this.tcpSessions.get(info.key);
                     if (session) {
                         session.protocol = "ssh";
                     }
@@ -324,7 +320,6 @@ export class QemuNetworkBackend extends EventEmitter {
                     }
                     return false;
                 }
-                const session = this.tcpSessions.get(info.key);
                 if (session) {
                     session.protocol = info.protocol;
                     if (info.protocol === "http" || info.protocol === "tls") {
